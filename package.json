{
  "name": "hardhat-project",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy-StakeSpacePi": "npx hardhat deploy --network mainnet --tags StakeSpacePi",
    "deploy-ERC721Distributor": "npx hardhat deploy --network mainnet --tags ERC721Distributor"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0",
    "merkletreejs": "^0.3.9"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "dotenv": "^16.0.3",
    "ethereum-waffle": "^4.0.10",
    "hardhat-deploy": "^0.11.26"
  }
}
